<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.football.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, phone, password, nickname, avatar, email, status, last_login_time,
        create_time, update_time, deleted
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE phone = #{phone} AND deleted = 0
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.football.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            username, phone, password, nickname, avatar, email, status,
            last_login_time, create_time, update_time, deleted
        ) VALUES (
            #{username}, #{phone}, #{password}, #{nickname}, #{avatar}, #{email}, #{status},
            #{lastLoginTime}, NOW(), NOW(), 0
        )
    </insert>

    <update id="updateById" parameterType="com.football.entity.User">
        UPDATE user
        SET
            username = #{username},
            phone = #{phone},
            password = #{password},
            nickname = #{nickname},
            avatar = #{avatar},
            email = #{email},
            status = #{status},
            last_login_time = #{lastLoginTime},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
