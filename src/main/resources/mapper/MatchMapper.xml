<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mapper.MatchMapper">

    <resultMap id="BaseResultMap" type="com.football.entity.Match">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="match_name" property="matchName" jdbcType="VARCHAR"/>
        <result column="match_type" property="matchType" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="start_time" property="startTime" jdbcType="TIMESTAMP"/>
        <result column="end_time" property="endTime" jdbcType="TIMESTAMP"/>
        <result column="description" property="description" jdbcType="TEXT"/>
        <result column="cover_image" property="coverImage" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, match_name, match_type, status, start_time, end_time, description,
        cover_image, create_time, update_time, deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `match`
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `match`
        WHERE deleted = 0
        <if test="query.matchName != null and query.matchName != ''">
            AND match_name LIKE CONCAT('%', #{query.matchName}, '%')
        </if>
        <if test="query.matchType != null and query.matchType != ''">
            AND match_type LIKE CONCAT('%', #{query.matchType}, '%')
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
        ORDER BY create_time DESC
        <if test="query.pageNum != null and query.pageSize != null">
            LIMIT #{query.pageSize} OFFSET #{(query.pageNum - 1) * query.pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*)
        FROM `match`
        WHERE deleted = 0
        <if test="query.matchName != null and query.matchName != ''">
            AND match_name LIKE CONCAT('%', #{query.matchName}, '%')
        </if>
        <if test="query.matchType != null and query.matchType != ''">
            AND match_type LIKE CONCAT('%', #{query.matchType}, '%')
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
    </select>

    <insert id="insert" parameterType="com.football.entity.Match" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `match` (
            match_name, match_type, status, start_time, end_time, description,
            cover_image, create_time, update_time, deleted
        ) VALUES (
            #{matchName}, #{matchType}, #{status}, #{startTime}, #{endTime}, #{description},
            #{coverImage}, NOW(), NOW(), 0
        )
    </insert>

    <update id="updateById" parameterType="com.football.entity.Match">
        UPDATE `match`
        SET
            match_name = #{matchName},
            match_type = #{matchType},
            status = #{status},
            start_time = #{startTime},
            end_time = #{endTime},
            description = #{description},
            cover_image = #{coverImage},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE `match`
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteByIds">
        UPDATE `match`
        SET deleted = 1, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

</mapper>
