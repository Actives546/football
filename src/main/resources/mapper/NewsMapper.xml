<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mapper.NewsMapper">

    <resultMap id="BaseResultMap" type="com.football.entity.News">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="cover_image" property="coverImage" jdbcType="VARCHAR"/>
        <result column="category" property="category" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="view_count" property="viewCount" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, cover_image, category, content,
        status, view_count, create_time, update_time, deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `news`
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `news`
        WHERE deleted = 0
        <if test="query.title != null and query.title != ''">
            AND title LIKE CONCAT('%', #{query.title}, '%')
        </if>
        <if test="query.category != null and query.category != ''">
            AND category = #{query.category}
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
        ORDER BY create_time DESC
        <if test="query.pageNum != null and query.pageSize != null">
            LIMIT #{query.pageSize} OFFSET #{query.offset}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*)
        FROM `news`
        WHERE deleted = 0
        <if test="query.title != null and query.title != ''">
            AND title LIKE CONCAT('%', #{query.title}, '%')
        </if>
        <if test="query.category != null and query.category != ''">
            AND category = #{query.category}
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
    </select>

    <insert id="insert" parameterType="com.football.entity.News" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `news` (
            title, cover_image, category, content,
            status, view_count, create_time, update_time, deleted
        ) VALUES (
            #{title}, #{coverImage}, #{category}, #{content},
            #{status}, 0, NOW(), NOW(), 0
        )
    </insert>

    <update id="updateById" parameterType="com.football.entity.News">
        UPDATE `news`
        SET
            title = #{title},
            cover_image = #{coverImage},
            category = #{category},
            content = #{content},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE `news`
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteByIds">
        UPDATE `news`
        SET deleted = 1, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

    <update id="updateStatus">
        UPDATE `news`
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateStatusBatch">
        UPDATE `news`
        SET status = #{status}, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

</mapper>
