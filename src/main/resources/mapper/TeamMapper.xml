<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mapper.TeamMapper">

    <resultMap id="BaseResultMap" type="com.football.vo.TeamVO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="team_name" property="teamName" jdbcType="VARCHAR"/>
        <result column="logo_url" property="logoUrl" jdbcType="VARCHAR"/>
        <result column="region" property="region" jdbcType="VARCHAR"/>
        <result column="founding_date" property="foundingDate" jdbcType="DATE"/>
        <result column="home_stadium" property="homeStadium" jdbcType="VARCHAR"/>
        <result column="head_coach" property="headCoach" jdbcType="VARCHAR"/>
        <result column="contact_person" property="contactPerson" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, team_name, logo_url, region, founding_date, home_stadium, 
        head_coach, contact_person, status, description, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `team`
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `team`
        WHERE deleted = 0
        <if test="query.teamName != null and query.teamName != ''">
            AND team_name LIKE CONCAT('%', #{query.teamName}, '%')
        </if>
        <if test="query.region != null and query.region != ''">
            AND region LIKE CONCAT('%', #{query.region}, '%')
        </if>
        <if test="query.status != null">
            AND status = #{query.status}
        </if>
        ORDER BY create_time DESC
        <if test="query.pageNum != null and query.pageSize != null">
            LIMIT #{query.pageSize} OFFSET #{query.offset}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*)
        FROM `team`
        WHERE deleted = 0
        <if test="query.teamName != null and query.teamName != ''">
            AND team_name LIKE CONCAT('%', #{query.teamName}, '%')
        </if>
        <if test="query.region != null and query.region != ''">
            AND region LIKE CONCAT('%', #{query.region}, '%')
        </if>
        <if test="query.status != null">
            AND status = #{query.status}
        </if>
    </select>

    <insert id="insert" parameterType="com.football.entity.Team" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `team` (
            team_name, logo_url, region, founding_date, home_stadium,
            head_coach, contact_person, status, description,
            create_time, update_time, deleted
        ) VALUES (
            #{teamName}, #{logoUrl}, #{region}, #{foundingDate}, #{homeStadium},
            #{headCoach}, #{contactPerson}, #{status}, #{description},
            NOW(), NOW(), 0
        )
    </insert>

    <update id="updateById" parameterType="com.football.entity.Team">
        UPDATE `team`
        SET
            team_name = #{teamName},
            logo_url = #{logoUrl},
            region = #{region},
            founding_date = #{foundingDate},
            home_stadium = #{homeStadium},
            head_coach = #{headCoach},
            contact_person = #{contactPerson},
            status = #{status},
            description = #{description},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE `team`
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteByIds">
        UPDATE `team`
        SET deleted = 1, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `team`
        WHERE deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `team`
        WHERE team_name = #{teamName} AND deleted = 0
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
        LIMIT 1
    </select>

</mapper>
